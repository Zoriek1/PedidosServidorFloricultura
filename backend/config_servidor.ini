[SERVIDOR]
# Hostname mDNS do servidor (usado para acesso via nome ao invés de IP)
# Este hostname será incluído nos certificados SSL
# Padrão: Gestor-pedidos.local
# 
# IMPORTANTE: 
# - Use apenas letras, números e hífens
# - Sempre termine com .local
# - Exemplo: Minha-Loja.local
#
hostname = Gestor-pedidos.local

[REDE]
# Porta do servidor (padrão: 5000)
porta = 5000

